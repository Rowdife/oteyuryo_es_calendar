# 日経時価総額ランキング スクレイピング実装

**日付**: 2025-11-30
**担当**: Parker + Claude Code
**ステータス**: ✅ 完了

---

## 📌 タスクの概要

### 目的
日本経済新聞の時価総額ランキングページから、上位200社の企業情報を取得し、CSV形式で出力する基盤を構築する。

### 背景
- 「上場企業ESシートカレンダー」プロジェクトの第一歩として、ターゲット企業リストが必要
- 時価総額上位企業は就活生にとって人気企業であり、ES締切情報のニーズが高い
- 手動での企業リスト作成は非効率なため、自動化が必要

### スコープ
- **対象**: 日経時価総額ランキング上位200社
- **取得データ**: ランキング、企業名、証券コード、時価総額、市場区分
- **出力形式**: CSV（UTF-8、カンマ区切り）
- **実装範囲**: スクレイピング + パース + CSV出力
- **対象外**: 企業詳細情報の取得（業種、従業員数など）は今後のタスク

---

## 🛠️ 技術的決定事項

### 採用した技術・ライブラリ

| 技術/ライブラリ | 用途 | 選択理由 |
|----------------|------|----------|
| **TypeScript** | 開発言語 | 型安全性、保守性向上 |
| **axios** | HTTP クライアント | シンプルで信頼性が高い |
| **cheerio** | HTML パーサー | jQuery ライクで使いやすく、軽量 |
| **csv-writer** | CSV 出力 | ヘッダー定義が明示的で扱いやすい |
| **Jest** | テストフレームワーク | TypeScript との統合が良好 |

### 設計方針
- **TDD（テスト駆動開発）**: テストファーストで実装し、リファクタリングの安全性を確保
- **XP（ペアプログラミング）**: 開発者 + Claude Code のペアプロ体制
- **責務の分離**:
  - `parser.ts`: HTML パース専任
  - `scraper.ts`: ページ取得 + 集約ロジック
  - `csvExporter.ts`: CSV 出力専任
- **エラー処理**: ページ取得失敗時も次ページを試行し、全体を停止させない

### 重要なデータ構造

```typescript
// 企業データの型定義（CSV スキーマと完全対応）
interface CompanyData {
  rank: number;                    // ランキング順位 (1-200)
  company_name: string;            // 企業名（日本語）
  ticker: string;                  // 証券コード
  market_cap: string;              // 時価総額（文字列）
  listing_market: string;          // 市場区分 (Prime/Standard/Growth)

  // 以下は将来的に取得予定（現時点では空欄）
  industry: string;
  foundation_year: string;
  employees: string;
  is_newgrad_active: string;
  hq_location: string;
  official_site_url: string;
  official_career_url: string;
  logo_image_url: string;
  revenue: string;
  operating_income: string;
  notes: string;
}
```

### HTML構造の発見
日経のページは JavaScript で動的レンダリングしているが、`__NEXT_DATA__` スクリプトタグに JSON データが埋め込まれていることを発見。cheerio で直接 JSON を抽出することで、複雑な DOM 操作を回避できた。

```javascript
// __NEXT_DATA__ 内の構造
{
  props: {
    pageProps: {
      data: {
        data_lists: [
          {
            RANK: 1,
            BICD: "7203",
            SOBA_NAME: "トヨタ",
            MKCP: "50,228,060,122,800",
            LISS_MKTN: ["MPR", "TP"],
            // ...
          }
        ]
      }
    }
  }
}
```

---

## 🐛 遭遇した問題と解決策

### 問題 1: listing_market が一部企業で空欄になる

**発覚時点**: 初回スクレイピング実行後、CSV を確認した際に発見

**症状**:
- 200社中、約190社で `listing_market` が空欄
- トヨタや三菱UFJなど一部の企業のみ "Prime" が設定されていた

**原因**:
`LISS_MKTN` 配列の構造を誤解していた。

初期実装では配列の **最初の要素のみ** をチェックしていたが、実際のデータは以下3パターン存在：
1. `["MPR", "TP"]` - 明示的にPrimeコード付き（6社）
2. `["TP"]` - Tokyo Prime の略？（191社・大半）
3. `["TPF"]` - Tokyo Prime Foreign（1社: YTL）

パターン2・3が最も多いのに、"TP" と "TPF" を Prime として認識していなかった。

**解決策**:
1. 実際のページデータをデバッグスクリプトで詳細に調査
2. `extractMarketCategory` 関数を修正:
   - 配列全体を検索するロジックに変更
   - 優先順位: `MPR` > `MST` > `MGR` > `TP` / `TPF`
3. ソニーG（6758）の市場区分を日経の個別ページで確認 → Prime と判明
4. YTL（1773）を調査 → マレーシア企業で「プライム外国」に上場と判明
5. テストケースを追加:
   - `["TP"]` → Prime
   - `["TPF"]` → Prime
   - 複数要素配列のパターン

**修正後の実装**:
```typescript
export function extractMarketCategory(marketCodes: string[] | undefined): string {
  if (!marketCodes || marketCodes.length === 0) return '';

  // 優先順位順にチェック
  if (marketCodes.includes('MPR')) return 'Prime';
  if (marketCodes.includes('MST')) return 'Standard';
  if (marketCodes.includes('MGR')) return 'Growth';
  if (marketCodes.includes('TP') || marketCodes.includes('TPF')) return 'Prime';

  return '';
}
```

**学び**:
- **データ構造の事前調査が重要**: 仮定に基づく実装は危険
- **実データでのテストが不可欠**: フィクスチャだけでは不十分
- **外れ値の確認**: 1件でも空欄があれば原因を調査すべき
- **TDD の効果**: テストがあったからこそ、修正後の安全性が保証された

---

## ✅ 実装の概要

### 主要な機能
1. **複数ページのスクレイピング** - page=1〜7 を順次取得
2. **HTMLパース** - `__NEXT_DATA__` からJSON抽出
3. **市場区分の変換** - コードから人間が読める形式へ変換
4. **CSV出力** - 指定スキーマに従った出力
5. **エラーハンドリング** - ページ取得失敗時も継続

### ファイル構成
```
ninki_es_calendar/
├── src/
│   ├── types.ts           # CompanyData, ScraperConfig などの型定義
│   ├── parser.ts          # HTMLパース + 市場区分変換ロジック
│   ├── scraper.ts         # メインスクレイピングロジック（複数ページ取得）
│   └── csvExporter.ts     # CSV出力機能
├── tests/
│   ├── parser.test.ts     # パーサーのテスト（17テストケース）
│   └── fixtures/
│       └── sample-page.html  # テスト用HTMLサンプル
├── output/
│   └── popular_companies.csv  # 出力結果（200社）
├── task_history/
│   ├── history_generating_rules.md
│   └── 2025-11-30_scraping-nikkei-ranking.md  # このファイル
├── package.json
├── tsconfig.json
├── jest.config.js
└── README.md
```

### テスト
- **方針**: TDD（テストファーストで実装）
- **結果**: 17/17 テストパス ✅
- **カバレッジ**: パーサーロジックは全てテストでカバー

### 動作確認

#### スクレイピング実行
```bash
npm run scrape
```

**結果**:
```
=== Nikkei Market Cap Ranking Scraper ===

Starting scraping: Target 200 companies across 7 pages
Fetching page 1: https://www.nikkei.com/marketdata/ranking-jp/market-cap-high/?page=1
Page 1: Found 30 companies
...
Page 7: Found 20 companies
Reached target of 200 companies

Total companies scraped: 200
Exported 200 companies to ./output/popular_companies.csv

✓ Successfully exported to ./output/popular_companies.csv
```

#### CSV 出力結果
- **総行数**: 201行（ヘッダー + 200社）
- **listing_market**: 200社全てで "Prime" が設定 ✅
- **データ品質**:
  - rank: 1〜200 の連番
  - company_name: 日本語企業名
  - ticker: 4桁証券コード
  - market_cap: カンマ区切りの文字列

**サンプル**:
```csv
rank,company_name,ticker,market_cap,listing_market,...
1,トヨタ,7203,"50,228,060,122,800",Prime,...
2,三菱ＵＦＪ,8306,"29,577,959,464,920",Prime,...
3,ソニーＧ,6758,"28,904,110,031,500",Prime,...
```

---

## 🔜 次回への引き継ぎ事項

### 未完了のタスク
- [ ] 各企業の詳細情報を取得（業種、従業員数、本社所在地など）
- [ ] 企業の採用サイトURLを自動収集
- [ ] ロゴ画像の取得
- [ ] Supabase へのデータ投入

### 既知の問題
なし（現時点で動作は正常）

### 今後の拡張案
1. **企業詳細ページのスクレイピング**
   - 日経の個別企業ページから追加情報を取得
   - 各企業の公式サイトから採用情報を抽出
2. **データ更新の自動化**
   - 定期的に時価総額ランキングを再取得
   - 変動があった企業を検出
3. **エラーリトライ機能**
   - ネットワークエラー時の自動リトライ
   - 指数バックオフの実装
4. **ログ機能の強化**
   - 構造化ログ（JSON形式）
   - ログレベルの設定

### 技術的な注意点
- **スクレイピング頻度**: 現在は1秒間隔でリクエスト。過度な負荷をかけないよう注意
- **HTML構造の変化**: 日経のサイト構造が変わる可能性あり。定期的な動作確認が必要
- **robots.txt の確認**: スクレイピング対象が規約違反でないか継続的に確認

---

## 📚 参考リンク
- [日経時価総額ランキング](https://www.nikkei.com/marketdata/ranking-jp/market-cap-high/)
- [東京証券取引所 - 市場区分](https://www.jpx.co.jp/equities/market/market-segments/index.html)
- [cheerio ドキュメント](https://cheerio.js.org/)
- [csv-writer (npm)](https://www.npmjs.com/package/csv-writer)

---

## 📊 統計情報

- **開発時間**: 約1セッション
- **作成ファイル数**: 9ファイル
- **テストケース数**: 17
- **スクレイピング対象**: 7ページ
- **取得企業数**: 200社
- **市場区分パターン発見**: 3種類（MPR/TP, TP, TPF）
